model Role {
  id          Int       @id @default(autoincrement())
  name        String    @unique
  type        String    @unique
  description String
  users       User[]
  createdAt   DateTime  @default(now())
  updatedAt   DateTime? @updatedAt
}
